<% if @draft.id? %>
    <!--<input type="submit" name="commit" value="Publish" form="edit_draft_<%= @draft.id %>" class="ui primary button disabled" id="publish-button" data-disable-with="Publish" />-->
<% else %>
    <!--<input type="submit" name="commit" value="Publish" form="new_draft" class="ui primary button disabled" id="publish-button" data-disable-with="Publish" />-->
<% end %>

<div class="ui teal buttons">
  <div class="ui button">Publish</div>
  <div class="ui floating dropdown icon button">
    <i class="dropdown icon"></i>
    <div class="menu">
      <div class="item"><i class="edit icon"></i> Edit Post</div>
      <div class="item"><i class="delete icon"></i> Remove Post</div>
      <div class="item"><i class="hide icon"></i> Hide Post</div>
    </div>
  </div>
</div>